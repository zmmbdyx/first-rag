# ===== 前端环境变量（可选）=====
# 只有需要把前端与服务端分开部署时才需要配置；默认留空即可。
#
# 默认行为：所有请求走相对路径 /api
#   - 开发态：由 vite.config.ts 的 proxy 转发到 VITE_BACKEND_URL
#   - 生产态：由 Nginx 把 /api 反代到后端
#
# 如需前端直连后端（跨域），取消下面注释并把后端地址加进
# 后端的 CORS_ORIGINS：
# VITE_API_BASE=http://127.0.0.1:8000/api
#
# 仅供 vite dev server 的 proxy 使用，不会打进前端产物：
# VITE_BACKEND_URL=http://127.0.0.1:8000

# ===== 服务端鉴权（仅当后端配置了 API_KEYS 时需要）=====
# 后端 API_KEYS 留空时不鉴权，此项无需设置。
#
# ⚠️ 注意：以 VITE_ 开头的变量会被**打进前端产物**，等于公开可见。
# 它适合"服务端对前端本身做访问控制"的场景；若你希望密钥完全不出现在
# 浏览器里，请在 Nginx 反代时注入 Authorization 头，而不要用这个变量。
# 另外：大模型的真实密钥（API_KEY）绝不能放在这里。
# VITE_API_KEY=sk-your-service-key
