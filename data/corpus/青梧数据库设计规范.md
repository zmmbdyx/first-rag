***REMOVED*** 青梧数据库设计规范

***REMOVED******REMOVED*** 一、命名规范

表名使用小写下划线风格并以模块为前缀，字段同样使用小写下划线。每张表必须包含created_at和updated_at字段。

***REMOVED******REMOVED*** 二、索引规范

索引以idx_为前缀，单表索引数量不超过5个。新索引上线前须用慢查询日志验证收益。

***REMOVED******REMOVED*** 三、大字段

超过500字节的文本字段拆分到扩展表，图片与文件一律存对象存储，数据库只存引用地址。