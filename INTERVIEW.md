***REMOVED*** 面试准备手册（基于星辰知识库 RAG 项目）

> 所有数据均为项目实测，可在 GitHub 仓库（eval/results/）现场验证。标注【按实际填写】的地方请替换为你的真实情况。

---

***REMOVED******REMOVED*** 一、简历版项目描述

***REMOVED******REMOVED******REMOVED*** 版本 A：精简版（简历项目经历栏，两行）

> **星辰知识库——企业级 RAG 智能问答系统**（Python · Chroma · 混合检索 + Reranker · 多厂商 LLM 路由）
> 独立设计开发；结构感知切分 + 向量/BM25 双路召回 + CrossEncoder 精排 + 段落级溯源；243 题评测端到端准确率 97.1%，溯源准确率 98.1%，含 36 项单元测试与可复现评测体系。

***REMOVED******REMOVED******REMOVED*** 版本 B：标准版（STAR 法则，4-5 行）

> **背景**：企业制度/合同/产品手册散落在 PDF、Word 等文档中，人工检索效率低，直接用大模型又会编造。
> **任务**：独立设计并实现一套可溯源、可量化评估的企业知识库 RAG 问答系统。
> **行动**：自研核心链路——多格式文档结构化解析（含表格/跨页合并）、章节感知智能切分、向量+BM25 双路召回经 RRF 融合再 CrossEncoder 精排、多轮查询改写、引用编号校验与注入拦截；同步构建 243 题评测集与自动化评测框架。
> **结果**：端到端准确率 97.1%（基线 74.4%），检索 Recall@5 99.5%，段落级溯源准确率 98.1%，拒答正确率 94.6%；每层优化的贡献均有消融实验和 95% 置信区间支撑，代码与评测报告全部开源可复现。

***REMOVED******REMOVED******REMOVED*** 版本 C：详细版（口述脚本，约 90 秒）

> "这个项目源于一个很实际的问题：公司的制度、合同、产品手册散落在各种 PDF 和 Word 里，找个答案要翻很久，直接问大模型它又会一本正经地编造。所以我想做一套**答案有出处、效果可量化**的知识库问答系统。
>
> 架构上是经典 RAG，但我在三个地方做了加深。**第一是切分**：我没有按固定字数切，而是先解析文档结构——PDF 按字体大小识别标题、Word 识别样式、表格单独抽取并做跨页合并——然后按'文档>章节'的树来切，块不超过 420 字，相邻块留一句重叠，表格按行分组且每组都带表头。这样每块天然带着溯源元数据。
>
> **第二是检索**：向量召回解决语义，BM25 解决型号编号这类精确匹配，两路用 RRF 融合——只比排名不比分数量纲，省掉归一化调参；融合后再过一个本地 CrossEncoder 精排。Reranker 带来的提升是实打实的，头部命中率从 78.5% 提到 87.4%。
>
> **第三，也是我最看重的：评测驱动**。我构建了 243 题的评测集，覆盖单跳、跨文档多跳、同义改写、错误前提对抗和拒答题，指标用 Recall/MRR 加 LLM 判分，所有指标都做了 bootstrap 置信区间，关键改进做了显著性检验。最终端到端准确率 97.1%，每层模块的贡献都被单独量化过——比如智能切分贡献 12.8 个点，混合检索 4.7 个点且 p=0.006。
>
> 工程上还做了多轮查询改写（'那转正后呢'能自动改写成独立问题）、答案引用编号校验（模型编造不存在的编号会触发重生成）、提示注入拦截和审计日志。整个项目在 GitHub 上开源，评测报告、探针数据全部可复现。"

【按实际填写】项目时长建议如实填写（如"3 周，业余时间迭代开发"）；如果你还想继续迭代，可以加一句"目前在持续迭代表格问答和重排序推理优化"。

---

***REMOVED******REMOVED*** 二、面试高频技术问答（20 题）

***REMOVED******REMOVED******REMOVED*** 架构与选型

**Q1 为什么选 Chroma？对比过哪些方案？**
- 推荐：选型时列了三个候选。FAISS 是库不是服务，持久化、元数据过滤、按文档删除都要自己搭；Milvus 功能全但运维重，我的场景单机几十万块以下用不上；Chroma 嵌入式零运维、自带持久化和 metadata 过滤，而我"同一文档重复入库先删后插"的幂等设计正好依赖它的 where 删除。我在代码里预留了 Milvus 迁移脚本和按文档哈希分片的方案，超过 10 万块可以平滑迁。
- 加分：指出 Chroma 的 HNSW 参数与余弦空间选择，以及"嵌入归一化 + 余弦距离"让相似度只看方向不受长度影响。

**Q2 为什么不用 Elasticsearch？向量和关键词检索的优劣？**
- 推荐：ES 一套就能做 BM25+向量混合，但对我这个单机、几万块的语料太重了——JVM、索引调优、运维成本都不划算。我用 jieba+BM25Okapi 自建关键词路，好处是分词完全可控（四种分词变体+自定义词典做过消融）。优劣上：向量管语义和同义改写，但对型号、编号、专有名词不敏感；BM25 恰好相反。所以两路都要，用 RRF 融合——只比排名不比分数量纲，免去两种分数归一化的调参。
- 加分：ES 的真正优势在分布式、高并发和成熟生态，如果语料到百万级、多团队共用，我会换成 ES/Milvus，代码里检索器是接口化的，替换成本可控。

**Q3 分块为什么是 420 字？overlap 怎么定？**
- 推荐：420 字对应嵌入模型 text2vec 的有效编码窗口（512 token，中文大约 400-500 字），超过这个长度语义会稀释。但我的切分不是"先定大小再硬切"：先按标题树切章节保证不跨章节，章节内按段落贪心打包到 420 字，单段超长再按句子边界下切，相邻块留 1 句重叠（大约 50-80 字）——overlap 的单位是句子不是字符，保证截断点语义完整。表格特殊处理：按行分组、每组带表头、跨页合并。
- 加分：这些参数不是拍的，是消融出来的——固定窗口基线 vs 结构感知切分，243 题上 R@5 差 12.8 个点，而且基线的失败案例全是答案被切在半截。

**Q4 Embedding 模型怎么选的？**
- 推荐：text2vec-base-chinese。三个理由：中文企业制度类语料表现稳定；本地跑，检索和评测零调用成本、可离线复现，数据不出内网；模型 100MB 级，CPU 就能实时编码查询。对比过 OpenAI text-embedding-3（效果好但要联网、按量付费、数据出境问题）和 BGE 系列（可用，效果接近）。关键是入库和查询必须同一个模型同一向量空间。加载走的 LangChain HuggingFaceEmbeddings 封装，方便以后换模型只改配置。
- 加分：做过 ONNX int8 量化对比，嵌入吞吐提升 1.47 倍，大规模入库时可以切换。

***REMOVED******REMOVED******REMOVED*** 检索优化

**Q5 检索不准怎么办？你做了哪些优化？**
- 推荐：我的方法论是"先定位再优化"。建了带金标的评测集，把每个 bad case 归因到检索 miss 还是生成错误，再用消融实验隔离每层贡献。数据说话：基线（固定窗口+纯向量）准确率 74.4%，结构感知切分解决"答案被切半截"提升最大，混合检索解决型号编号精确匹配（R@5 +4.7pp，p=0.006 显著），CrossEncoder 重排提升头部排序（R@1 +8.9pp）。每一层贡献都被单独量化过，不是拍脑袋堆模块。
- 加分：负结果也测过——LLM 查询扩展在我这语料上是负收益（R@3 -0.7pp），如实记录并默认关闭。

**Q6 Query 改写怎么做的？**
- 推荐：针对多轮对话的指代消解，"规则+LLM"两步式。规则层用信号词（那/它/该/上述等）加历史判断是否需要改写，需要时 LLM 把口语输入改写成独立可检索的问题——比如"那转正后呢"会改写成"转正后的试用期考核标准是什么"。LLM 失败自动降级到规则改写（把上一轮主题拼接进来）。多轮题端到端准确率从 75% 提到 88%。
- 加分：改写结果同时用于检索和生成，前端会展示改写后的查询，用户能感知到系统"听懂了"。

**Q7 Reranker 的原理？为什么能提升？**
- 推荐：向量检索是双塔结构，query 和文档各自独立编码成向量再算相似度，快但精度有限——两个文本没有充分交互。CrossEncoder 把 query 和文档拼在一起过同一个 Transformer，逐 token 做注意力交互，判断"这段话是否回答了这个问题"要准得多。代价是每对都要跑一次前向，所以我只用它对融合后的 top20 精排再取 top5。实测头部命中率 +8.9 个点。
- 加分：成本控制——20 个候选在 CPU 上 1.5 秒，我做了 ONNX 量化路径（嵌入层实测 1.47 倍加速）可以平移到重排；langchain-community 0.4 已经移除了它们的压缩器组件，我直接用它的同源实现 sentence-transformers CrossEncoder，这个选型判断被面试官问起也是加分项。

**Q8 知识库没有答案的问题怎么处理？**
- 推荐：三层。提示词层面强制"资料里没有就直接说未找到，不要猜"；生成后校验引用编号合法性，实质性回答却零引用会触发重生成；评测层面专门有 37 道拒答题量化这个能力——知识库外问题拒答正确率 94.6%。有个反面教训很有价值：我的基线系统拒答率只有 23.5%，归因发现是固定窗口切块把无关章节的内容混进了上下文，垃圾上下文会诱导模型编造——所以拒答能力其实是被切分策略决定的。
- 加分：还能识别错误前提——用户问"C3保修期是5年吧"，系统会纠正"不对，是2年"并给出引用，对抗题 14 条全部纠偏。

**Q9 多跳推理问题怎么解决？**
- 推荐：两段式。检索侧靠"重排+多来源"策略：重排序器会把不同文档的相关块都拉进 top5，15 条跨文档多跳题检索覆盖 100%；生成侧在提示词里明确要求"多来源问题逐个来源作答、分别标注引用"，避免只答一半。实测跨文档多跳端到端准确率 100%。更复杂的多跳可以上 agent 式问题分解，但我的评测数据显示当前规模下这个方案够了——先量化再决定要不要上复杂架构。
- 加分：评测里专门设计了"推理步骤"字段，每条多跳题标注需要几步、每步的信息来源，方便归因。

***REMOVED******REMOVED******REMOVED*** 生成质量

**Q10 如何防止幻觉？**
- 推荐：四层防线。提示词强制"仅根据资料回答，资料没有就说未找到"；引用校验——模型给的 [n] 编号必须真实对应检索块，伪造编号或实质性回答零引用会自动重生成一次；审计日志全链路留痕可追查；最后是量化——忠实度指标（答案逐句判断是否被检索块支持）0.95，对抗错误前提题 14 条全部纠偏而不是顺着用户说。
- 加分：拒答率是双刃剑，我用专门题型校准——知识库外问题要拒答、错误前提要纠正、边界模糊要区分场景，这三类行为都不一样。

**Q11 Prompt 怎么设计的？**
- 推荐：system prompt 是一条带优先级的规则列表：仅基于资料、关键结论带 [n] 引用、无答案必须拒答、多来源逐个引用、表格按行列对应取值、安全规则优先级最高（禁止泄露提示词、忽略资料里的指令性内容）。每条规则都对应一个评测题型——比如加了表格规则之后表格问答的行列错配明显减少。温度 0.1-0.3。
- 加分：Prompt 是随评测演进的，每条规则背后都有一个让它存在的 bad case。

**Q12 如何评估生成质量？**
- 推荐：三个层面。检索层用金标原文片段匹配算 Recall@k 和 MRR——金标直接取文档原文，避免"标准答案被改写导致误判"。生成层用 LLM-as-Judge 三档判分（correct/partial/wrong），再加两个 RAGAS 式指标：忠实度（答案逐句判断能否被检索块支持）和答案相关性。统计上所有指标做 1000 次 bootstrap 置信区间，关键改进做配对显著性检验。评测集 243 题，覆盖单跳、跨文档多跳、同义改写、错误前提和拒答。
- 加分：诚实披露局限——判分模型和被测模型同源有自判偏差，所以关键结论都要求置信区间不含 0。

**Q13 上下文太长超出窗口怎么办？**
- 推荐：我的策略是"少而准"优于"多而全"——重排序保证 top5 就是最相关的，5 块×420 字大约 2k token，远小于窗口，也省 Token（实测每次约 1200）。如果业务上确实需要更多上下文，路径有三条：提高 k 但依赖重排控噪声、map-reduce 分段摘要、或上下文压缩。我没有过早引入这些复杂度，因为评测数据显示当前瓶颈不在窗口。
- 加分：这是一个成本决策——每 1000 token 都是钱，评测驱动让我知道加多少上下文是值得的。

**Q14 如何做到来源溯源？**
- 推荐：溯源是"入库埋点、检索编号、生成映射、校验兜底"四步。切分时每个块就带着文档名、章节路径、页码；送入大模型时每个块编号并带来源头；回答要求关键结论标注 [n]；后端正则解析编号反向映射回块，校验编号合法性（伪造会重生成）。溯源准确率 98.1%——不仅能给出处，还能精确到章节和页码。
- 加分：审计日志记录每次问答的完整 prompt、检索结果和引用映射，事后可追查，这对企业内部合规很重要。

***REMOVED******REMOVED******REMOVED*** 工程与性能

**Q15 索引构建耗时？增量更新怎么做的？**
- 推荐：纯 CPU 实测 1000 篇/5000 块全量入库 200 秒。增量靠 MD5 清单：入库时记每个文件的哈希和切分器版本，重跑时内容没变的文件直接跳过——1000 篇重跑只要 1.25 秒，150 倍加速。同一文档重复入库是先删后插的幂等操作，不会堆积脏数据。
- 加分：解析和切分阶段做了多线程，嵌入做过 ONNX int8 量化对比（1.47 倍加速），10 万块级有 Milvus 迁移脚本。

**Q16 并发请求怎么处理？**
- 推荐：当前 Streamlit 形态是单机演示，但核心包按线程安全设计的：模型和客户端是进程级单例、Token 用量用线程本地存储、评测里实测过 6 线程并发跑 244 题。服务化路径是把 pipeline.chat 套 FastAPI——核心包零 UI 依赖，迁移成本低。需要重点处理的是 SQLite 指标落库的并发写锁（我踩过这个坑，异常捕获已按 SQLite 错误类型修正）。
- 加分：能说出"Streamlit 适合演示、生产应该 FastAPI+uvicorn workers，向量库和指标存储才是水平扩展的关键点"这个分层判断。

**Q17 线上出过什么问题？怎么排查的？**
- 推荐：讲一个最有代表性的：评测时发现三个配置的召回率一模一样（都是 57.7%），这不符合直觉。排查发现是评测集的问题——金标答案是我改写过的，不是文档原文，子串匹配全部失效，指标等于随机。重构了评测方法学：金标一律取文档原文片段，并且生成测试集时自动校验"片段⊆文档"。这件事让我建立了"指标先可信、再谈优化"的原则。另一个是 LLM 免费配额在评测中途耗尽，403 错误打断了整轮评测——后来加了按配置落盘、单题失败不中断、配额错误不重试这些健壮性设计。
- 加分：两个案例都是"现象反直觉→定位到根因→变成系统性设计"的完整闭环。

***REMOVED******REMOVED******REMOVED*** 反思与改进

**Q18 如果重新做一遍，会怎么改进？**
- 推荐：三件事。第一，先建评测集再写代码——我是做到一半才补评测的，导致前期一些优化方向靠感觉，评测建立后每次改动都有数据背书。第二，直接上 FastAPI+Docker 而不是 Streamlit，把服务化成本前置。第三，表格问答会一开始就设计专用链路（行列序列化+专用提示词），它至今是准确率最低的题型（62.5%），根因是通用链路对结构化数据不友好。
- 加分：强调"改进方向都有数据支撑"，比如表格短板是从题型分项里精确发现的。

**Q19 目前最大的瓶颈是什么？**
- 推荐：两个。功能上，表格/结构化数据的问答（对比、聚合类问题）准确率明显低于文本——通用 RAG 链路对表格的行列关系建模不够，需要专用方案。工程上，重排序的 CPU 延迟（1.5 秒）限制了并发吞吐，需要 GPU 或 ONNX 量化。另外判分模型与被测模型同源的自判偏差，是评测体系公认的局限，我在报告里如实写了。
- 加分：每个瓶颈都配了"下一步计划"，说明不是停在抱怨。

---

***REMOVED******REMOVED*** 三、可能被追问的"坑题"（5 题）

**坑题 1："你说不准，那准确率到底怎么算的？"（怀疑指标注水）**
- 回答思路：不躲闪，直接给方法学。①评测集 243 题，金标答案全部取文档**原文片段**，生成测试集时自动校验"片段⊆文档"；②检索指标用子串精确匹配，生成指标用 LLM 三档判分（correct/partial/wrong），同时报"严格准确率"和"correct+半对"两个口径；③所有指标有 1000 次 bootstrap 的 95% 置信区间，关键改进做配对显著性检验（如混合检索 p=0.006）；④如实披露局限：判分与被测同源、语料是程序化生成的 22 篇文档、绝对数值在真实大规模语料上会回落。
- 关键：主动说出局限反而建立可信度。

**坑题 2："如果知识库有 100 万篇文档，你这套还能用吗？"**
- 回答思路：分层回答，不笼统说行或不行。检索层：向量库换 Milvus（迁移脚本已备），BM25 全库打分 O(N) 会成瓶颈，换 Elasticsearch；嵌入层：批量走 API 或 ONNX 批处理；重排层成本不变（永远只重排 top20）；架构层：按业务域分片多索引+路由。增量管道（MD5+manifest）天然支持持续灌库。最后补一句：评测集也要按新语料重新构造，指标不能沿用。
- 关键：展示"知道现有方案的天花板在哪、每一层怎么换"。

**坑题 3："你的方案和直接让 GPT 回答有什么区别？"**
- 回答思路：三点。私有知识不在预训练语料里——模型没见过我们的制度细节，直接问必然编造；时效性——文档更新当天就能生效，不用等模型更新；可溯源可审计——每个答案带文档章节页码引用，企业内部合规必需。数据支撑：我的基线测试里，无检索的拒答/编造行为对比，以及"检索到垃圾上下文会让拒答率掉到 23.5%"这个反例，说明关键不只是"有没有检索"，而是"检索质量"。
- 关键：用实测数据把"RAG 必要性"从概念落到证据。

**坑题 4："RAG 和微调你怎么选？"**
- 回答思路：先问场景再给结论。知识频繁更新、要求可溯源、要控制幻觉 → RAG；要改变模型的输出风格/领域语言/固定格式 → 微调；两者可以叠加（微调过的模型做 RAG 的生成端）。成本上 RAG 边际成本低（更新文档=重新入库几秒），微调每次知识更新都要重训。我的场景是制度文档经常更新，所以 RAG 是必选项。
- 关键：体现"按问题特征选技术"而不是"手里拿锤子看啥都是钉子"。

**坑题 5："你遇到过最难的 Bug 是什么？"**
- 回答思路（STAR）：最难的不是代码 bug，是**指标 bug**。现象：三种检索配置的召回率完全相同，不符合直觉。排查：先怀疑检索实现，逐层加日志看候选集——发现每层的检索结果其实有差异，问题出在评测环节：金标答案是改写过的表述而非文档原文，子串匹配全部失效，所有配置都"命中失败"，指标失去区分度。修复：重构评测方法学——金标一律取文档原文片段、多段用分号连接、要求同一召回块包含全部片段，并且生成测试集时机器校验"片段⊆文档"。修复后指标立刻拉开差距（88.5%→100%），后续所有优化决策才有了可信依据。教训：RAG 项目里评测管道的正确性比模型参数更重要。
- 关键：展示"对指标本身保持怀疑"的工程素养。

---

***REMOVED******REMOVED*** 四、项目亮点提炼（面试主动展示，引导提问方向）

1. **评测驱动开发，每层优化有消融数据**："基线 74.4% → 智能切分 +12.8pp → 混合检索 +4.7pp（p=0.006）→ 重排 R@1 +8.9pp"，每一步都有置信区间和显著性检验，负结果（查询扩展无收益）也如实入册。
2. **段落级溯源 + 引用校验闭环**：答案标注文档·章节·页码（溯源准确率 98.1%），模型伪造引用编号会被检测并自动重生成——"可信"不是口号，是校验出来的。
3. **安全与审计完整**：提示注入拦截（实测）、注入诱导类对抗题全部防御、全链路审计日志，面向企业内部合规设计。
4. **工程效率**：MD5 增量入库 150 倍加速（1000 篇 1.25 秒）、嵌入 ONNX int8 加速 1.47 倍、多厂商 LLM 路由（模型名@端点别名，一套代码切换 DeepSeek/通义/智谱）。
5. **完全可复现**：代码、243+70 题两代评测集、全部评测报告和探针数据开源在 GitHub，面试官可以现场验证任何一条数据。
