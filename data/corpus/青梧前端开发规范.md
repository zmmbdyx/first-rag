***REMOVED*** 青梧前端开发规范

***REMOVED******REMOVED*** 一、命名规范

React组件文件与组件名使用大驼峰命名，工具函数文件使用小驼峰，常量全部大写。

***REMOVED******REMOVED*** 二、目录结构

页面放在src/pages下，公共组件放src/components，请求封装统一放src/api，禁止在页面里直接写请求地址。

***REMOVED******REMOVED*** 三、状态管理

跨页面共享的状态使用全局状态管理，仅组件内部使用的状态不得提升到全局。构建产物目录为dist，禁止提交到代码仓库。